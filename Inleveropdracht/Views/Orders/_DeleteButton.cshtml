@model OrderItemViewModel

<form method="post" asp-action="DeleteProduct" asp-route-productId="@Model.ProductId" onsubmit="return confirm('Are you sure you want to delete this product?')">
    <button type="submit" class="btn btn-danger">
        Verwijder
    </button>
</form>
